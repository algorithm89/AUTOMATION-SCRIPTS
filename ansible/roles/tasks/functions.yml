
---

- name: DIR
  become: true
  vars_prompt:
    - name: FOLDER
      prompt: "Which folder to look into?"
      private: no
           

  hosts: all
  

  tasks:
     - name: CHECK CONTENT INSIDE FOLDER
       shell: "ls -la {{ FOLDER }}"
